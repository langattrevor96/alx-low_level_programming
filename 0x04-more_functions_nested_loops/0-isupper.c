#include "main.h"
/**
 * _isupper - checks if letters are uppercase
 * @c: is input
 * Return: 1 if c is uppercase otherwise 0
 */
int _isupper(int c)
{
	if ((c >= 'A') && (C <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
